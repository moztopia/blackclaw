# Legacy Flutter application

This inherited scaffold is not part of the Darkclaw public website or Census client
matrix. Its status screen calls the Laravel website API directly. It must not use the
canonical Census contract as though Laravel implemented Census..
